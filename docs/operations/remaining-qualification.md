# Remaining owner qualification

Use a fresh vault containing only synthetic values. Preserve the package receipt and exact OS version. Record fixed outcomes, durations and hashes; no passwords, sign-in codes, tokens, DOM or raw network logs belong in evidence.

## Live ChatGPT connection

1. Open the verified local app, create a test vault, and choose Agent Access, then Sign in with ChatGPT.
2. Complete device sign-in in the owner's browser. Shadow must show ChatGPT connected; cancelling or locking during sign-in must leave no pending authority.
3. Start a harmless task such as asking Codex to compute `17 * 19` with its Python tool. Confirm the actual result is `323`, the task ends and its enrolled caller disappears.
4. Exercise sign-out and reauthentication through native controls. Qualify token refresh without exposing the token or opening another application's credential store.

This uses the ChatGPT subscription flow. An OpenAI API key is not required.

## Physical OS events and owner controls

With the owner ready to unlock the Mac, run separate active-task trials for screen lock, sleep/wake, app Quit, and forced termination. Confirm every old grant/reference/session fails after interruption and reopening begins locked. Measure independent browser/egress lease closure within the ten-second bound; use only synthetic fixture instrumentation. Do not infer OS notification delivery from an injected notification test.

Use keyboard and VoiceOver to reach import mapping, separate catalog/use approvals, retained-copy warning, revoke, recovery review, sign-in cancellation and the private challenge view. Confirm the account, caller, exact destinations, actions and expiry can be understood before approving.

## Recovery

Follow [the recovery guide](recovery.md) using synthetic entries. Inspect the export independently in the qualified KeePassXC. Rehearse both intact-history restore and a fresh synthetic installation without a restriction ledger; confirm mirrored entries need retained-copy review and no previous grant revives. Keep all sole encrypted copies and record owner acceptance separately.

## Real site and external review

Choose a site and read-only task before implementing its adapter. Qualify its exact credential/resource origins, redirects, MFA, permitted pages, safe fields and failure behavior using the production browser image. Real credentials remain blocked until G1–G6 and separate owner rollout approval.

Give the independent reviewer the source revision, compatibility manifest, package receipt, threat model and requirement evidence matrix. Record their findings, resolutions and signed-off limitations. A passing local code review does not close that gate.
