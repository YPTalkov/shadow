# Connector implementation status

The private Python consumer implements the pinned JSON wire profile in the source contract. Transactions use bounded trusted memory, a host-supplied HMAC key, authenticated enrollment/epoch parameters, source generations, and encrypted receipts/provenance. Success follows the ordinary anchored KDBX transaction. No producer can supply approval fields, clear restrictions or set host freshness.

Synthetic tests cover exact replay, changed batch replay, stale generations, malformed/oversized/unauthenticated frames, interrupted batches, concurrent owner writes, disk full, coverage contradictions, distinct source identities with duplicate labels, multiple memberships, scope loss, retained deletion, reappearance, and all conflict choices. The consumer's first run exposed PyKeePass's refusal of None in its TOTP setter; empty TOTP now uses the library's supported empty string.

Unlinked candidates stay blocked. Owner adoption creates a distinct local UUID and retains the archived encrypted candidate. Source conflict data and baseline fields stay encrypted and are included in known-secret projection checks. Native editor reconciliation preserves app-owned metadata at entry, group and vault level.

The native restriction service supports idempotent batches of up to 256 immutable events under a separate Keychain anchor. Missing expected history fails closed. Private worker callbacks invalidate authority before recording restrictions and before publishing KDBX mutations. Native catalog projection creates restrictions for stale or uncertain mirrors and preserves them after source reappearance. The ledger is created with mode 0600, and its directory is synchronized before its independent anchor advances.

The native Sources screen inspects a signed connector app without executing it, shows its capabilities and executable fingerprint, and requires owner enrollment. Refresh verifies its sealed resources, exact executable version and running process CDHash before sending a collection request over an inherited socket pair. A Keychain digest key never reaches the producer. Debug environments and stderr are excluded. Disabling/removing enrollment preserves encrypted accounts. Refresh is manual; no background schedule is enabled. Conflict resolution and retained-item summaries are native-only controls.

The signed synthetic producer test exercises actual process launch, channel exchange, KDBX publication, disabled enrollment, cancellation during preparation, changed bundle rejection and removal retention. The owner UI probe also enrolls/refreshes/removes this producer, then reviews and applies an unchanged mixed CSV/source vault in the editor workflow. That integration exposed two bugs now covered by regressions: relative/absolute Foundation executable URLs compared differently, and PyKeePass returns None for empty custom fields that must be restored as empty strings.

An active protected-browser authentication interrupted by source deletion still needs the U9–U13 integrated test. Packaged identity/entitlement qualification and independent review remain release gates. This is not release approval.

Verification: the connector/catalog/private-worker Python slice passed 38 tests; connector/editor tests passed after the empty-field fix; all six CSV tests passed, including Unicode frame budgets. The Swift suite includes native source publication/reappearance/reopen, launch identity, a 256-event restriction batch, replay, missing history and multi-page owner metadata. `owner-ui-probe` passed with the signed synthetic bundle; `.build/evidence/owner-sources.jpg` was visually inspected. These local artifacts contain synthetic data only.
